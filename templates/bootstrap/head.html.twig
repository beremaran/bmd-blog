<!DOCTYPE html>
<html lang="en">
<head>
    <!-- Required meta tags -->
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no"/>

    <title>{{ pageTitle }} - {{ blogTitle }}</title>

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="{{ site_url }}/{{ template }}/css/bootstrap.min.css"/>
    <link rel="stylesheet" href="{{ site_url }}/{{ template }}/css/md-blog.css"/>
</head>
<body>
